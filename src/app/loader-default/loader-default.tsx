import "@styles/loader-default/loader-default.scss"

const LoaderDefault = () => {
    return (
        <div className="loader-default">
            
        </div>
    )
}

export default LoaderDefault
